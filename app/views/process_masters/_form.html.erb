<%= form_for(@process_master) do |f| %>
  <% if @process_master.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@process_master.errors.count, "error") %> prohibited this process_master from being saved:</h2>

      <ul>
      <% @process_master.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div id="form-holder">
    <div class="row">
      <div class="col-md-6">
        <div class="form-group">
          <label class="col-md-12 control-label">Select Action</label>
          <input type="text" placeholder="Action" class="col-md-12">
        </div>
      </div>
    </div>
  </div>
  
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

<script>
  
</script>
