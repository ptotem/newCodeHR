<%= form_for(@manpower_planning) do |f| %>
  <% if @manpower_planning.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@manpower_planning.errors.count, "error") %> prohibited this manpower_planning from being saved:</h2>

      <ul>
      <% @manpower_planning.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
