<%= form_for(@notification_master) do |f| %>
  <% if @notification_master.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@notification_master.errors.count, "error") %> prohibited this notification_master from being saved:</h2>

      <ul>
      <% @notification_master.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
