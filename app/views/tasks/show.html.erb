<p id="notice"><%= notice %></p>

<div class="row">
  <div class="col-md-6">
    <div class="table-header">
      Notification Master Details :-
    </div>
    <table class="table table-striped table-bordered table-hover">
      <tbody>
          <tr>
            <th>Title</th>
            <td><%= @task.title %></td>
          </tr>
          <tr>
            <th>Description</th>
            <td><a href="<%= @task.link %>"><%= raw @task.description %></a></td>
          </tr>
      </tbody>
    </table>
  </div>
</div>


<%= link_to 'Back', tasks_path %>

